import { ENTITY_ORGANIZATION_ACTIVITIES } from "@/src/features/auth/constants/registerEntity";

/** Only backend-backed service options are exposed at runtime. */
export const ORGANIZATION_ACTIVITY_OPTIONS = ENTITY_ORGANIZATION_ACTIVITIES;
export const ORGANIZATION_DESCRIPTION_MAX_LENGTH = 4000;
