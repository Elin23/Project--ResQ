import { Ionicons } from "@expo/vector-icons";
import { Pressable, StyleSheet, View } from "react-native";
import AppText from "@/src/components/ui/AppText";
import { COLORS, FONT_SIZES, FONTS, RADIUS, SPACING } from "@/src/theme";
import type { ProfileMenuSection as Section } from "../types/profile";
export default function ProfileMenuSection({section,onPress}:{section:Section;onPress:(route?:import("expo-router").Href,label?:string)=>void}) {return <View style={s.wrap}><AppText style={s.heading}>{section.title}</AppText><View style={s.card}>{section.items.map((item,index)=><Pressable key={item.id} onPress={()=>onPress(item.route,item.label)} style={[s.row,index<section.items.length-1&&s.border]}><Ionicons name="chevron-back" size={20} color={COLORS.brownMuted}/><View style={s.valueWrap}>{item.value&&<AppText color={COLORS.warning}>{item.value}</AppText>}<AppText style={s.label}>{item.label}</AppText><Ionicons name={item.icon} size={23} color={item.color}/></View></Pressable>)}</View></View>}
const s=StyleSheet.create({wrap:{gap:SPACING.sm,marginBottom:SPACING.lg},heading:{fontFamily:FONTS.medium,fontSize:FONT_SIZES.title,color:COLORS.brownMuted,textAlign:"right"},card:{backgroundColor:COLORS.white,borderRadius:RADIUS.xl,borderWidth:1,borderColor:COLORS.border,overflow:"hidden"},row:{height:58,paddingHorizontal:SPACING.md,flexDirection:"row",alignItems:"center",justifyContent:"space-between"},border:{borderBottomWidth:1,borderBottomColor:COLORS.offwhite},valueWrap:{flex:1,flexDirection:"row",alignItems:"center",justifyContent:"flex-end",gap:SPACING.sm},label:{fontFamily:FONTS.medium,fontSize:FONT_SIZES.body}});
