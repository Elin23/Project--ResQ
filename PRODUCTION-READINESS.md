# PRODUCTION-READINESS — تطبيق ResQ

هذه الوثيقة تحدّد حدود الجاهزية للإصدار الحالي من التطبيق، وما الذي يفصله عن نسخة إنتاجية متصلة بخادم حقيقي.

---

## V1 Mobile RC integration boundary

الإصدار الحالي هو **مرشّح إصدار للواجهة الأمامية (Mobile RC)**، وليس نسخة إنتاجية.

**حدّ التكامل:** التطبيق يعمل بالكامل على مستودعات بيانات في الذاكرة (`src/data/repositories/inMemory*`) عبر مفتاح `useMockApi: true` في `src/constants/config.ts`. لم يتم ربط أي واجهة برمجية خلفية بعد.

**ما هو مكتمل ومختبَر:**
- جميع شاشات التطبيق (72 شاشة) وتدفّقاتها ومنطق عرضها.
- قواعد الصلاحيات لأنواع الحسابات (زائر / مستخدم / جهة) وبواباتها.
- عقود التصميم: الألوان والخطوط والمسافات والاتجاه العربي RTL، مفروضة آلياً.
- طبقة النطاق (`src/domain`) والتطبيق (`src/application`) معزولة عن أطر الواجهة.
- 71 اختبار وحدة، فحص أنواع نظيف، وسلسلة فحوصات معمارية آلية.

**ما هو خارج نطاق هذا الإصدار:**
- ربط الواجهة بـ API الخلفي (ASP.NET Core) وقاعدة البيانات (SQL Server).
- المصادقة الحقيقية وإدارة الجلسات على الخادم.
- رفع الملفات والصور إلى تخزين دائم.
- الإشعارات الفورية (push notifications).
- خريطة Google الحقيقية على الأجهزة — مفتاح `GOOGLE_MAPS_API_KEY` غير مُعدّ بعد، وواجهة الويب تعرض معاينة ثابتة بديلة.
- الاختبار على أجهزة Android/iOS حقيقية — التحقق البصري تم على `expo start --web` فقط.

---

## خطوات الانتقال إلى الإنتاج

1. تنفيذ واجهات الـ API الخلفية المقابلة لعقود المستودعات في `src/data/repositories/`.
2. ضبط `apiUrl` وتحويل `useMockApi` إلى `false` في `src/constants/config.ts`.
3. نقل منطق قواعد العمل الموجود حالياً في المستودعات الوهمية إلى الخادم — تحديداً: اعتماد طلبات التبنّي، مراجعة نقاط الإطعام، والتحقق من التحويلات المالية.
4. توليد المعرّفات على الخادم بدل التوليد المحلي في المستودعات.
5. إعداد `GOOGLE_MAPS_API_KEY` وبناء نسخة تطوير (dev build) لاختبار الخريطة الأصلية.
6. اختبار على أجهزة حقيقية عبر مصفوفة الأحجام (320 / 360 / 390 / لوحي).

---

## ضمان الجودة الحالي

| الفحص | الحالة |
|---|---|
| `npm run typecheck` | نظيف — 538 ملفاً |
| `npm run lint` | 0 أخطاء، 9 تحذيرات غير حاجبة |
| `npm run test` | 71 اختباراً ناجحاً في 15 ملفاً |
| `npm run check` | سلسلة فحوصات معمارية آلية |

**دين موثّق ومؤجّل:** انظر القسم 8 من `CLAUDE.md`.
**جرد المكوّنات والتوكنز:** `AUDIT-INVENTORY.md`.
**جرد الشاشات:** `SCREEN-INVENTORY.md`.